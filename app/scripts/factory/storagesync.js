'use strict';

/*
	Storage Sync Factory
*/
angular.module('hackathon27112015TeamDApp')
  .factory('storageSyncService', ['$http', '$cookies', 'Restangular', 'localStorageService', function ($http, $cookies, Restangular, localStorageService) {
    	return {
    		currentVideo : function() {
    			
    		}
    	}
  }]);